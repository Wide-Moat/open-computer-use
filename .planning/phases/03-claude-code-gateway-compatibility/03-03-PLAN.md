---
phase: 03-claude-code-gateway-compatibility
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docker-compose.yml
  - .env.example
  - docs/claude-code-gateway.md
  - README.md
  - docs/INSTALL.md
autonomous: true
requirements:
  - GATEWAY-08
  - GATEWAY-09
  - GATEWAY-10
  - GATEWAY-11

must_haves:
  truths:
    - "docker-compose.yml computer-use-server.environment declares ANTHROPIC_AUTH_TOKEN, ANTHROPIC_BASE_URL, and all ten gateway vars in ${VAR:-} form (GATEWAY-MH-08)."
    - ".env.example contains a new 'Optional: Claude Code sub-agent gateway overrides' block with ten commented var lines (GATEWAY-MH-09)."
    - "docs/claude-code-gateway.md exists with SPDX header, three-path table, worked LiteLLM recipe, and verification checklist (GATEWAY-MH-10)."
    - "README.md 'Open WebUI Integration' section and docs/INSTALL.md .env section both cross-link docs/claude-code-gateway.md."
    - "docs/claude-code-gateway.md contains zero real-looking API keys; uses sk-EXAMPLE... placeholders."
  artifacts:
    - path: "docker-compose.yml"
      provides: "12 new environment entries under computer-use-server.environment"
      contains: "ANTHROPIC_AUTH_TOKEN=${ANTHROPIC_AUTH_TOKEN:-}"
    - path: ".env.example"
      provides: "New gateway-overrides section with 10 commented var lines"
      contains: "# === Optional: Claude Code sub-agent gateway overrides ==="
    - path: "docs/claude-code-gateway.md"
      provides: "Operator-facing gateway configuration guide"
      min_lines: 80
      contains: "claude-code-gateway"
    - path: "README.md"
      provides: "Cross-link to docs/claude-code-gateway.md from Open WebUI Integration section"
      contains: "claude-code-gateway.md"
    - path: "docs/INSTALL.md"
      provides: "Cross-link to docs/claude-code-gateway.md from .env configuration table"
      contains: "claude-code-gateway.md"
  key_links:
    - from: "docker-compose.yml"
      to: "host environment (.env)"
      via: "${VAR:-} substitution"
      pattern: "ANTHROPIC_AUTH_TOKEN=\\$\\{ANTHROPIC_AUTH_TOKEN:-\\}"
    - from: "README.md / docs/INSTALL.md"
      to: "docs/claude-code-gateway.md"
      via: "markdown relative link"
      pattern: "docs/claude-code-gateway.md"

user_setup: []
---

<objective>
Ship the operator-facing surface for Phase 3:

1. **Critical bug fix in `docker-compose.yml`** — the `computer-use-server.environment:` section currently does NOT declare `ANTHROPIC_AUTH_TOKEN` or `ANTHROPIC_BASE_URL` (verified against checked-in file). Without them the orchestrator container never sees those vars from `.env`, so Path B is broken end-to-end even with the 03-01 context-var fix. Add them plus all ten new gateway vars.

2. **`.env.example` expansion** — add a new commented `# === Optional: Claude Code sub-agent gateway overrides ===` block with ten `# VAR=` placeholder lines, per D7.

3. **New `docs/claude-code-gateway.md`** — operator-facing guide with SPDX header, three-path purpose table, worked LiteLLM recipe (with `sk-EXAMPLE...` placeholders), Azure/Bedrock-via-LiteLLM cross-reference, verification checklist, and troubleshooting for issue #40.

4. **Cross-links** — one-sentence pointers from `README.md` (Open WebUI Integration section) and `docs/INSTALL.md` (.env configuration table) to the new doc.

Purpose: close the operator UX loop — make the knobs discoverable (`.env.example`, `docker-compose.yml`), actually wired (`docker-compose.yml`), and explained (`docs/claude-code-gateway.md`). This plan is independent of plan 03-02 (tests) so they can run in parallel in wave 2 after plan 03-01.

Output: two config files updated, one new doc created, two existing docs touched with a one-sentence cross-link each. No Python code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-claude-code-gateway-compatibility/03-CONTEXT.md
@.planning/phases/03-claude-code-gateway-compatibility/03-RESEARCH.md
@.planning/phases/03-claude-code-gateway-compatibility/03-01-SUMMARY.md
@CLAUDE.md
@docker-compose.yml
@.env.example
@docs/INSTALL.md
@docs/openwebui-filter.md
@README.md

<interfaces>
<!-- Extracted from codebase. Match these patterns verbatim. -->

Current `docker-compose.yml` `computer-use-server.environment:` section (lines 36-46):

```yaml
environment:
  - MCP_API_KEY=${MCP_API_KEY:-}
  - DOCKER_IMAGE=${DOCKER_IMAGE:-open-computer-use:latest}
  - USER_DATA_BASE_PATH=/tmp/computer-use-data
  - BASE_DATA_DIR=/tmp/computer-use-data
  - COMMAND_TIMEOUT=${COMMAND_TIMEOUT:-120}
  - SUB_AGENT_TIMEOUT=${SUB_AGENT_TIMEOUT:-3600}
  - SINGLE_USER_MODE=${SINGLE_USER_MODE:-}
  - VISION_API_KEY=${VISION_API_KEY:-}
  - VISION_API_URL=${VISION_API_URL:-}
  - VISION_MODEL=${VISION_MODEL:-}
```

Current `.env.example` Claude Code section (lines 51-53):

```
# === Optional: Claude Code sub-agent ===
# ANTHROPIC_AUTH_TOKEN=
# ANTHROPIC_BASE_URL=https://api.anthropic.com
```

Current `docs/INSTALL.md` Configuration table (lines 30-38) — the row `ANTHROPIC_AUTH_TOKEN` already exists under the 'No' required column.

Current `README.md` section header `## Open WebUI Integration` at line 187.

Doc template structure (from `docs/openwebui-filter.md`): Purpose / Installation / Feature explanation / Valves reference table / Troubleshooting.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire all twelve env vars into docker-compose.yml (GATEWAY-08)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (full file — you will edit only the `computer-use-server` service's `environment:` list, lines 36-46)
    - .planning/phases/03-claude-code-gateway-compatibility/03-CONTEXT.md (D7 — exact var names and ${VAR:-} convention)
    - .planning/phases/03-claude-code-gateway-compatibility/03-RESEARCH.md (section "docker-compose.yml (current state)" — documents the missing-vars bug)
  </read_first>
  <action>
Edit `docker-compose.yml`. Under `services.computer-use-server.environment:`, after the existing `- VISION_MODEL=${VISION_MODEL:-}` line (currently line 46) and before `restart: unless-stopped` (line 47), append exactly these twelve lines, indented to match (6-space indent for the list items):

```yaml
      - ANTHROPIC_AUTH_TOKEN=${ANTHROPIC_AUTH_TOKEN:-}
      - ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL:-}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-}
      - ANTHROPIC_DEFAULT_SONNET_MODEL=${ANTHROPIC_DEFAULT_SONNET_MODEL:-}
      - ANTHROPIC_DEFAULT_OPUS_MODEL=${ANTHROPIC_DEFAULT_OPUS_MODEL:-}
      - ANTHROPIC_DEFAULT_HAIKU_MODEL=${ANTHROPIC_DEFAULT_HAIKU_MODEL:-}
      - CLAUDE_CODE_SUBAGENT_MODEL=${CLAUDE_CODE_SUBAGENT_MODEL:-}
      - CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=${CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS:-}
      - DISABLE_PROMPT_CACHING=${DISABLE_PROMPT_CACHING:-}
      - DISABLE_PROMPT_CACHING_SONNET=${DISABLE_PROMPT_CACHING_SONNET:-}
      - DISABLE_PROMPT_CACHING_OPUS=${DISABLE_PROMPT_CACHING_OPUS:-}
      - DISABLE_PROMPT_CACHING_HAIKU=${DISABLE_PROMPT_CACHING_HAIKU:-}
```

Do NOT change any other service (cleanup, workspace, volume declarations). Do NOT change the `VISION_*` block ordering. Do NOT change existing SPDX header lines 1-2. Do NOT add a blank separator line before or after the new block — the convention is continuous list items.

Verify the file still parses as valid YAML after the edit.
  </action>
  <verify>
    <automated>python -c "import yaml; y = yaml.safe_load(open('docker-compose.yml')); env = y['services']['computer-use-server']['environment']; required = ['ANTHROPIC_AUTH_TOKEN', 'ANTHROPIC_BASE_URL', 'ANTHROPIC_MODEL', 'ANTHROPIC_DEFAULT_SONNET_MODEL', 'ANTHROPIC_DEFAULT_OPUS_MODEL', 'ANTHROPIC_DEFAULT_HAIKU_MODEL', 'CLAUDE_CODE_SUBAGENT_MODEL', 'CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS', 'DISABLE_PROMPT_CACHING', 'DISABLE_PROMPT_CACHING_SONNET', 'DISABLE_PROMPT_CACHING_OPUS', 'DISABLE_PROMPT_CACHING_HAIKU']; missing = [r for r in required if not any(e.startswith(r + '=') for e in env)]; assert not missing, f'missing: {missing}'; print('OK:', len(env), 'env entries')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` exits 0 (valid YAML).
    - For each name in {`ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_BASE_URL`, `ANTHROPIC_MODEL`, `ANTHROPIC_DEFAULT_SONNET_MODEL`, `ANTHROPIC_DEFAULT_OPUS_MODEL`, `ANTHROPIC_DEFAULT_HAIKU_MODEL`, `CLAUDE_CODE_SUBAGENT_MODEL`, `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS`, `DISABLE_PROMPT_CACHING`, `DISABLE_PROMPT_CACHING_SONNET`, `DISABLE_PROMPT_CACHING_OPUS`, `DISABLE_PROMPT_CACHING_HAIKU`}: `grep -qE "^      - ${name}=\\\$\\{${name}:-\\}\$" docker-compose.yml` exits 0.
    - The existing line is preserved: `grep -q '      - VISION_MODEL=\${VISION_MODEL:-}' docker-compose.yml` exits 0.
    - Existing Open WebUI compose file is untouched: `diff docker-compose.webui.yml docker-compose.webui.yml` noop, and no mention of this file in the git diff output.
    - `docker compose config --services 2>/dev/null` (if `docker` is available in the environment) lists `computer-use-server` among the services; skip if docker not available.
    - `grep -q 'ANTHROPIC_AUTH_TOKEN' docker-compose.yml` now exits 0 (was absent before the edit).
  </acceptance_criteria>
  <done>docker-compose.yml `computer-use-server.environment:` section lists twelve new entries (2 previously-missing + 10 new) in the specified order directly after VISION_MODEL; file still parses as YAML; no other service touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append gateway-overrides block to .env.example (GATEWAY-09)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full file — current ending on line 58 with the GitLab block)
    - .planning/phases/03-claude-code-gateway-compatibility/03-CONTEXT.md (D7 — exact header and var list)
  </read_first>
  <action>
Edit `.env.example`. Immediately after line 53 (`# ANTHROPIC_BASE_URL=https://api.anthropic.com`) and before the `# === Optional: GitLab integration ===` block (line 55), insert exactly these thirteen lines (leading blank line for visual separation):

```

# === Optional: Claude Code sub-agent gateway overrides ===
# Pass-through to sandbox when set. Leave commented to use Claude Code defaults.
# ANTHROPIC_MODEL=
# ANTHROPIC_DEFAULT_SONNET_MODEL=
# ANTHROPIC_DEFAULT_OPUS_MODEL=
# ANTHROPIC_DEFAULT_HAIKU_MODEL=
# CLAUDE_CODE_SUBAGENT_MODEL=
# CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=
# DISABLE_PROMPT_CACHING=
# DISABLE_PROMPT_CACHING_SONNET=
# DISABLE_PROMPT_CACHING_OPUS=
# DISABLE_PROMPT_CACHING_HAIKU=
```

Preserve the existing two-line Claude Code section header block (`# === Optional: Claude Code sub-agent ===` and the two `# ANTHROPIC_AUTH_TOKEN=` / `# ANTHROPIC_BASE_URL=` lines). Preserve the GitLab section and everything below. Do not add or change any other line.

File MUST remain ASCII-only English.
  </action>
  <verify>
    <automated>grep -q '^# === Optional: Claude Code sub-agent gateway overrides ===$' .env.example && grep -q '^# Pass-through to sandbox when set. Leave commented to use Claude Code defaults.$' .env.example && for v in ANTHROPIC_MODEL ANTHROPIC_DEFAULT_SONNET_MODEL ANTHROPIC_DEFAULT_OPUS_MODEL ANTHROPIC_DEFAULT_HAIKU_MODEL CLAUDE_CODE_SUBAGENT_MODEL CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS DISABLE_PROMPT_CACHING DISABLE_PROMPT_CACHING_SONNET DISABLE_PROMPT_CACHING_OPUS DISABLE_PROMPT_CACHING_HAIKU; do grep -qE "^# ${v}=\$" .env.example || { echo "missing $v"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^# === Optional: Claude Code sub-agent gateway overrides ===$' .env.example` exits 0.
    - `grep -q '^# Pass-through to sandbox when set. Leave commented to use Claude Code defaults.$' .env.example` exits 0.
    - For each var in {`ANTHROPIC_MODEL`, `ANTHROPIC_DEFAULT_SONNET_MODEL`, `ANTHROPIC_DEFAULT_OPUS_MODEL`, `ANTHROPIC_DEFAULT_HAIKU_MODEL`, `CLAUDE_CODE_SUBAGENT_MODEL`, `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS`, `DISABLE_PROMPT_CACHING`, `DISABLE_PROMPT_CACHING_SONNET`, `DISABLE_PROMPT_CACHING_OPUS`, `DISABLE_PROMPT_CACHING_HAIKU`}: `grep -qE "^# ${var}=\$" .env.example` exits 0.
    - Existing sections still present: `grep -q '^# === Optional: Claude Code sub-agent ===$' .env.example` exits 0, `grep -q '^# === Optional: GitLab integration ===$' .env.example` exits 0.
    - File is ASCII-only: `LC_ALL=C grep -cP '[\x80-\xFF]' .env.example` returns `0`.
    - Line count increases by exactly 13 compared to pre-edit (was 58 → after edit 71).
  </acceptance_criteria>
  <done>.env.example has a new 13-line gateway-overrides block placed directly after the existing Claude Code block and before the GitLab block, using the exact header and commented var lines; file remains ASCII-only English.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create docs/claude-code-gateway.md — operator guide (GATEWAY-10)</name>
  <files>docs/claude-code-gateway.md</files>
  <read_first>
    - docs/openwebui-filter.md (structure template — mirror its Purpose / Options / Reference / Troubleshooting flow)
    - .planning/phases/03-claude-code-gateway-compatibility/03-CONTEXT.md (D6 — doc scope; Specifics — suggested structure)
    - .planning/phases/03-claude-code-gateway-compatibility/03-RESEARCH.md (section "Recommended structure for docs/claude-code-gateway.md" — operator-facing not contributor-facing)
    - CLAUDE.md (SPDX header requirement for new files; English-only)
  </read_first>
  <action>
Create NEW file `docs/claude-code-gateway.md`. First two lines MUST be the SPDX header:

```
<!-- SPDX-License-Identifier: BUSL-1.1 -->
<!-- Copyright (c) 2025 Open Computer Use Contributors -->
```

(HTML-comment form because Markdown doesn't have a native comment syntax; mirrors the convention used in other `docs/*.md` files if any, otherwise this is the project's first markdown SPDX — the form is standard.)

File structure (in this order; minimum 80 lines total):

### Section 1 — Title + intro

`# Claude Code Gateway Configuration`

One paragraph explaining: each sandbox container runs the Claude Code CLI as a sub-agent (`sub_agent` MCP tool); operators choose where Claude Code sends its API calls via host env vars; three paths are supported.

### Section 2 — Three-path table

A markdown table with columns `Path | Operator sets | Outcome`:

| Path | Operator sets on host | Claude Code inside sandbox |
|------|------------------------|----------------------------|
| **A — Zero-config** | Nothing | Shows native `/login` OAuth flow |
| **B — Public Anthropic** | `ANTHROPIC_AUTH_TOKEN` | Talks to `https://api.anthropic.com` with the supplied token |
| **C — Custom gateway** | `ANTHROPIC_AUTH_TOKEN` + `ANTHROPIC_BASE_URL` + optional compatibility flags | Talks to the gateway with the gateway-scoped token |

### Section 3 — Path A (zero-config)

Subheader `## Path A — Zero-config (stock Claude Code /login)`. Explain: leave all `ANTHROPIC_*` and `CLAUDE_CODE_*` env vars unset on the host; `docker compose up`; when the user triggers `sub_agent`, Claude Code will prompt for OAuth login in its interactive terminal. Reference the ttyd terminal in Open WebUI as the place users see the prompt.

### Section 4 — Path B (public Anthropic)

Subheader `## Path B — Public Anthropic with your own key`. Show a `.env` snippet with `sk-EXAMPLE-...` placeholder — NEVER a real-looking key:

```
ANTHROPIC_AUTH_TOKEN=sk-EXAMPLE-replace-with-your-anthropic-key
# ANTHROPIC_BASE_URL defaults to https://api.anthropic.com when unset
```

Explain: this is the simplest paid path; the token is scoped per Anthropic account; no gateway in the middle. Link to https://code.claude.com/docs/en/env-vars for the canonical doc.

### Section 5 — Path C (custom gateway)

Subheader `## Path C — Custom gateway (LiteLLM, Azure, Bedrock)`. Show a full `.env` recipe using the LiteLLM proxy — again `sk-EXAMPLE-...` placeholders only:

```
ANTHROPIC_AUTH_TOKEN=sk-EXAMPLE-litellm-master-key
ANTHROPIC_BASE_URL=https://litellm.internal/anthropic
CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1
DISABLE_PROMPT_CACHING=1
ANTHROPIC_DEFAULT_SONNET_MODEL=anthropic/claude-sonnet-4-6
ANTHROPIC_DEFAULT_OPUS_MODEL=anthropic/claude-opus-4-6
ANTHROPIC_DEFAULT_HAIKU_MODEL=anthropic/claude-haiku-4-5
```

Explain each var in a table; cross-reference https://code.claude.com/docs/en/llm-gateway for the canonical recipe.

Add one short sub-section "Azure / Bedrock via LiteLLM" explaining that Azure OpenAI and AWS Bedrock don't support Claude Code natively but can be fronted by a LiteLLM proxy that does; `ANTHROPIC_DEFAULT_*_MODEL` then points at LiteLLM-routed deployments like `azure/my-sonnet-deployment` or `bedrock/anthropic.claude-sonnet-4-20250514-v1:0`.

### Section 6 — Full variable reference

A table listing all twelve vars (the two auth + the ten new) with columns `Variable | Type | Default | Purpose | Example (placeholder)`. Examples must be `sk-EXAMPLE-*` for secrets, `anthropic/...` or `azure/...` for model IDs, and `1` or empty for flags.

### Section 7 — Verification checklist

Subheader `## Verifying your gateway setup`. Numbered steps including at least:

1. `grep -E '^(ANTHROPIC|CLAUDE_CODE)' .env` — confirm the vars are in the file.
2. `docker compose up -d computer-use-server` — recreate the orchestrator container.
3. `docker inspect computer-use-server --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -E '^(ANTHROPIC|CLAUDE_CODE)'` — confirm the orchestrator sees them.
4. Trigger a `sub_agent` call from the Computer Use tool in Open WebUI.
5. `docker exec owui-chat-<chatid> env | grep -E '^(ANTHROPIC|CLAUDE_CODE)'` — confirm the sandbox receives the expected subset.
6. If Path B/C: observe the sub-agent streaming responses without a `/login` prompt. If still prompting, proceed to Troubleshooting.

### Section 8 — Troubleshooting

Subheader `## Troubleshooting`. At least two entries:

- **"The sub-agent keeps asking me to /login even though I set `ANTHROPIC_AUTH_TOKEN`."** — diagnose: (1) confirm the orchestrator container actually received the env via the `docker inspect` step; if missing, ensure `ANTHROPIC_AUTH_TOKEN` is declared in `docker-compose.yml` `computer-use-server.environment:` and recreate the container; (2) confirm you are on a version with the context_vars.py fix (reference Phase 3 / issue #40); earlier versions had a ContextVar default that short-circuited the env fallback, so the sandbox received `ANTHROPIC_BASE_URL=https://api.anthropic.com/` but no `ANTHROPIC_AUTH_TOKEN`. Link to https://github.com/Yambr/open-computer-use/issues/40.

- **"LiteLLM returns 400 about prompt caching."** — LiteLLM generally doesn't support Anthropic's prompt-caching headers; set `DISABLE_PROMPT_CACHING=1` on the host.

### Section 9 — Further reading

Bullet list linking to the three canonical Claude Code doc pages:
- https://code.claude.com/docs/en/env-vars
- https://code.claude.com/docs/en/llm-gateway
- https://code.claude.com/docs/en/model-config

All prose English-only. No emojis. No real-looking keys — the string `sk-EXAMPLE` MUST appear at least 3 times; no other `sk-...` prefixed strings with 20+ base62 characters are permitted anywhere in the doc.
  </action>
  <verify>
    <automated>test -f docs/claude-code-gateway.md && head -2 docs/claude-code-gateway.md | grep -q 'SPDX-License-Identifier: BUSL-1.1' && [ "$(wc -l < docs/claude-code-gateway.md)" -ge 80 ] && grep -c 'sk-EXAMPLE' docs/claude-code-gateway.md | awk '{ exit $1 >= 3 ? 0 : 1 }' && [ "$(grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md)" = "0" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/claude-code-gateway.md` exits 0.
    - `head -2 docs/claude-code-gateway.md` contains `SPDX-License-Identifier: BUSL-1.1` and `Copyright (c) 2025 Open Computer Use Contributors`.
    - `wc -l < docs/claude-code-gateway.md` returns `80` or greater.
    - `grep -c 'sk-EXAMPLE' docs/claude-code-gateway.md` returns `3` or greater.
    - `grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md` returns `0` (no real-looking secrets).
    - `grep -q '^# Claude Code Gateway Configuration$' docs/claude-code-gateway.md` exits 0.
    - `grep -q '^## Path A' docs/claude-code-gateway.md` exits 0.
    - `grep -q '^## Path B' docs/claude-code-gateway.md` exits 0.
    - `grep -q '^## Path C' docs/claude-code-gateway.md` exits 0.
    - `grep -q '^## Verifying your gateway setup$' docs/claude-code-gateway.md` exits 0.
    - `grep -q '^## Troubleshooting$' docs/claude-code-gateway.md` exits 0.
    - `grep -q 'https://code.claude.com/docs/en/env-vars' docs/claude-code-gateway.md` exits 0.
    - `grep -q 'https://code.claude.com/docs/en/llm-gateway' docs/claude-code-gateway.md` exits 0.
    - `grep -q 'https://github.com/Yambr/open-computer-use/issues/40' docs/claude-code-gateway.md` exits 0.
    - `grep -q 'CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1' docs/claude-code-gateway.md` exits 0.
    - `grep -q 'DISABLE_PROMPT_CACHING=1' docs/claude-code-gateway.md` exits 0.
    - All 10 new env var names present: `for v in ANTHROPIC_MODEL ANTHROPIC_DEFAULT_SONNET_MODEL ANTHROPIC_DEFAULT_OPUS_MODEL ANTHROPIC_DEFAULT_HAIKU_MODEL CLAUDE_CODE_SUBAGENT_MODEL CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS DISABLE_PROMPT_CACHING DISABLE_PROMPT_CACHING_SONNET DISABLE_PROMPT_CACHING_OPUS DISABLE_PROMPT_CACHING_HAIKU; do grep -q "$v" docs/claude-code-gateway.md || exit 1; done`.
    - File is ASCII-only: `LC_ALL=C grep -cP '[\x80-\xFF]' docs/claude-code-gateway.md` returns `0`.
  </acceptance_criteria>
  <done>docs/claude-code-gateway.md exists with SPDX HTML-comment header, at least 80 lines, all nine sections present, at least three `sk-EXAMPLE` placeholder occurrences, no real-looking API keys, and all ten new env var names documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Cross-link docs/claude-code-gateway.md from README.md and docs/INSTALL.md (GATEWAY-11)</name>
  <files>README.md, docs/INSTALL.md</files>
  <read_first>
    - README.md lines 180-215 (Open WebUI Integration section boundaries)
    - docs/INSTALL.md lines 25-45 (Configuration section with the .env table)
    - .planning/phases/03-claude-code-gateway-compatibility/03-CONTEXT.md (D6 — one-sentence cross-link convention)
  </read_first>
  <action>
Two small edits.

**Edit A — README.md.** Inside the `## Open WebUI Integration` section (starts at line 187), after the introductory paragraphs but before the `The openwebui/ directory contains:` bullet list, add exactly ONE new paragraph on its own line:

```
Running Claude Code through a corporate gateway (LiteLLM, Azure, Bedrock)? See [docs/claude-code-gateway.md](docs/claude-code-gateway.md) for the three-path operator recipe.
```

Placement: find the line `**Why not a fork?** We intentionally did not fork Open WebUI.` ... paragraph. Insert the new paragraph with one blank line above and one blank line below it, directly AFTER that existing "Why not a fork?" paragraph and BEFORE the `The openwebui/ directory contains:` line. Do not reorder any other content.

**Edit B — docs/INSTALL.md.** Inside the `## Configuration` section (starts at line 26), directly after the line `See `.env.example` for the full list with defaults.` (currently line 40), insert one new line with a blank line above it:

```
Routing Claude Code through a custom gateway (LiteLLM / Azure / Bedrock)? See [claude-code-gateway.md](claude-code-gateway.md) for the full recipe.
```

Use a relative link — the file is in the same directory — so the link is simply `claude-code-gateway.md`.

Do NOT add any other edit to either file. No table-row additions, no section renames, no emoji, no date stamps.
  </action>
  <verify>
    <automated>grep -q 'docs/claude-code-gateway.md' README.md && grep -q 'claude-code-gateway.md' docs/INSTALL.md && grep -q 'corporate gateway' README.md && grep -q 'custom gateway' docs/INSTALL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'docs/claude-code-gateway.md' README.md` returns exactly `1`.
    - `grep -c 'claude-code-gateway.md' docs/INSTALL.md` returns exactly `1`.
    - The README link text contains "LiteLLM", "Azure", and "Bedrock": `grep -q 'LiteLLM.*Azure.*Bedrock' README.md` exits 0.
    - The README cross-link paragraph is located between the "Why not a fork?" paragraph and the "The openwebui/ directory contains:" line: `awk '/Why not a fork/,/The openwebui\/ directory contains:/' README.md | grep -q 'docs/claude-code-gateway.md'` exits 0.
    - The INSTALL cross-link is placed after the line `See \`.env.example\` for the full list with defaults.`: `awk '/See `.env.example`/,/## Model Settings/' docs/INSTALL.md | grep -q 'claude-code-gateway.md'` exits 0.
    - Neither file grew by more than 4 lines (single paragraph + surrounding blank lines): `git diff --stat README.md docs/INSTALL.md 2>/dev/null | awk '/README.md|INSTALL.md/ { total+=$3+$5 } END { exit total <= 8 ? 0 : 1 }'` exits 0 (or visually verify line delta ≤ 4 per file).
    - No table modifications: `grep -c '^| ' README.md` and `grep -c '^| ' docs/INSTALL.md` unchanged versus pre-edit baseline (executor verifies by checking the git diff shows only insertions, no line removals or modifications inside tables).
  </acceptance_criteria>
  <done>README.md has one new paragraph inside Open WebUI Integration pointing to docs/claude-code-gateway.md; docs/INSTALL.md has one new sentence inside Configuration pointing to the same doc (relative link); no other edits to either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc / config files → git history | Public repository; anything committed is public forever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | I (credential leak in docs) | docs/claude-code-gateway.md worked examples | mitigate | Acceptance criterion requires `grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md` to return 0 AND at least 3 occurrences of `sk-EXAMPLE`. |
| T-03-07 | T (env var misinterpretation) | docker-compose.yml `${VAR:-}` pattern | accept | Empty-string pass-through is handled by the `if value:` guard in docker_manager.py (exercised by the test plan's empty-string test). |
| T-03-08 | I (secret leak in .env.example) | .env.example new block | mitigate | Only `# VAR=` commented lines — no values, so nothing to leak. Acceptance criterion enforces. |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` exits 0.
- `docker compose config` (if docker is available locally) succeeds and shows the twelve new env entries on `computer-use-server`.
- `docs/claude-code-gateway.md` passes the no-real-keys check: `grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md` returns `0`.
- README.md and docs/INSTALL.md each contain exactly one cross-link to `claude-code-gateway.md`.
</verification>

<success_criteria>
- All four tasks' acceptance criteria pass.
- `./tests/test-no-corporate.sh` still passes (no corporate references introduced).
- `./tests/test-project-structure.sh` still passes.
- `grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md .env.example docker-compose.yml` returns `0` across all three files.
- Existing orchestrator tests unaffected: `python -m pytest tests/orchestrator/ -v` remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-gateway-compatibility/03-03-SUMMARY.md` following the standard summary template. Capture:
- The exact line ranges added to docker-compose.yml and .env.example.
- The final line count of docs/claude-code-gateway.md.
- The exact placement of both cross-link sentences (line number in README.md and docs/INSTALL.md).
- Output of `grep -cE 'sk-[A-Za-z0-9]{20,}' docs/claude-code-gateway.md` (must be 0).
</output>
