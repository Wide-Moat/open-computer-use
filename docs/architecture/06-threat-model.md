<!-- SPDX-License-Identifier: FSL-1.1-Apache-2.0 -->
<!-- Copyright (c) 2025 Open Computer Use Contributors -->

---
status: draft
last-reviewed: 2026-05-30
owner: "@Wide-Moat/architects"
applies-to: next/v1
---

STRIDE-per-element threat model over the Layer 6 container DFD, with each threat resolved to a canon NFR or routed to a tracked open hole. Audience: security engineers and auditors reviewing the trust-boundary controls.

## 1. Scope and DFD reference

This model runs STRIDE-per-element over the seven containers, two data stores, four external actors, and eleven boundary-crossing flows fixed in [`05-c4-container.md`](05-c4-container.md) §3–§4 and drawn in [`diagrams/c4-container.mmd`](diagrams/c4-container.mmd) — the DFD is not redrawn here. STRIDE letters apply per the Microsoft element-type mapping (process S/T/R/I/D/E; data-flow T/I/D plus the S/E attributable to the boundary it crosses; data-store T/I/D, plus R only for the audit log; external entity S/R only); severity is qualitative Likelihood × Impact, and each row anchors to an NFR-SEC target with the per-zone framework cell from [`02-trust-boundaries.md`](02-trust-boundaries.md) §11. The model inverts the usual ingress focus: the primary adversary is the in-sandbox process (P5, holding in-sandbox root), so the high-value rows weight P5's outbound and host-facing edges, not the inbound caller edge.

## 2. Element inventory and STRIDE applicability

| Element | Type | Applicable STRIDE |
|---|---|---|
| P1 MCP gateway | process | S T R I D E |
| P2 Control / operator API | process | S T R I D E |
| P3 Credential custody | process | S T R I D E |
| P4 Storage broker | process | S T R I D E |
| P5 Session sandbox [1..N] | process | S T R I D E |
| P6 Egress trust-edge proxy | process | S T R I D E |
| P7 Audit pipeline | process | S T R I D E |
| D1 Credential custody store | data store | T I D |
| D2 Audit pipeline store | data store (log) | T I D R |
| F1–F11 boundary flows | data flow | T I D (+ S/E per boundary) |
| E1 caller · E2 operator · E3 IdP · E4 SOAR | external | S R |

E1, E2, and E4 carry their S/R threats inline (P1, P2 rows). E3 (the customer IdP) is a relying-party trust anchor whose own spoofing/repudiation is owned by the customer's identity estate, outside the OCU boundary; its failure surfaces here only as the F3 availability dependency (P2-D2).

## 3. STRIDE-per-element threats and mitigations

| ID | Element/edge | STRIDE | Threat | L | I | Rating | Mitigation (arch) | Anchor (NFR + framework) | Residual | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| P1-S1 | F1 caller→gateway | S | Rogue client replays/forges a bearer token, or presents a wrong-audience token, to open/address sessions it does not own. | M | H | High | Gateway is a relying party validating issuer/audience/expiry against the customer IdP; identity never self-asserted in the body. Full shelf uses the customer IdP, minimal shelf a host-rooted local credential. | NFR-SEC-09 + FLEX-03 + SEC-10; NYDFS §500.7/§500.12, DORA Art.30, EU AI Act Art.13, CCM IAM-08 | Audience binds token to the deployment, not to one session; per-tool/per-action authz unstated. | PARTIAL |
| P1-S2 | F5 gateway→Control API | S | Gateway forges/escalates a service identity on F5 so the Control API treats a session request as a more privileged principal. | L | H | Med | Control API authenticates the gateway service principal, not an operator; kill-switch and lifecycle live only on operator-only ingress, split enforced by network policy and distinct privilege set, not an in-process guard. | NFR-SEC-26 + §3 split; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | Minimum scope of the gateway's internal token on F5 is unstated. | PARTIAL |
| P1-T1 | F1 caller→gateway | T | On-path attacker rewrites tool-call parameters (container_name, args, selector) in flight. | L | M | Med | F1 is TLS 1.3; gateway validates audience/integrity before acting; metadata-only and credential-free, so a tampered request cannot escalate beyond the validated caller, and host-derived F6 identity is the downstream authority. | NFR-SEC-33; NYDFS §500.15, DORA Art.30, EU AI Act Art.13, CCM IAM-08 | Parameter-schema validation depth is component-spec, not pinned. | PARTIAL |
| P1-T2 | F5 gateway→Control API | T | Gateway crafts an F5 body claiming another tenant's session_id/container_name to bind or read a session it does not own. | M | H | High | Host-attested identity invariant: a body-supplied id is a hint cross-checked against host-derived identity; Control API derives the binding and the host dials the guest on F6; cross-session reachability disabled. | NFR-SEC-43; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | Cross-check binds the gateway service identity, not the end-caller's tenant; per-tool/action authz unstated. | PARTIAL |
| P1-R1 | E1 caller (action attribution; via F1) | R | Caller denies issuing a tool-call/session-create; no independent record attributes the action. | L | M | Med | Gateway emits an OCSF event on F11 per terminated request with validated caller identity; hash-chained store, daily Merkle head to a transparency log. | NFR-SEC-03; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01 | Ordering depends on a trustworthy clock (#185); metadata-only gate not yet measurable (#149). | PARTIAL |
| P1-I1 | F1 caller→gateway | I | Verbose MCP errors / tool-discovery leak session ids, container_names, tenant ids, or the operator interface to a low-privilege caller. | M | M | Med | Gateway is metadata-only and holds no credential or kill-switch state, bounding the leak; structured (not free-text) responses per the egress deny-reason discipline; F1 TLS 1.3. | NFR-SEC-33; NYDFS §500.7, DORA Art.30, EU AI Act Art.13, CCM IAM-08 | Error-surface minimization and no-payload-on-control-plane gate not yet measurable (#149). | OPEN |
| P1-D1 | F1 caller→gateway | D | Caller floods the agent-facing MCP surface, exhausting gateway connections/CPU and indirectly pressuring the lifecycle plane via F5. | H | M | High | Gateway is a separate runnable unit from the Control API; MCP saturation cannot reach operator ingress or the kill-switch (distinct network policy); per-tenant MCP-calls/min + concurrent-session quota bounds the flood and the F5 spillover. | NFR-COST-06 + SEC-01; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | No connection-ceiling / availability SLO for the gateway socket; quotas are per-tenant aggregate, not per-caller connection-level. | PARTIAL |
| P1-D2 | F5 gateway→Control API | D | Each accepted MCP request fans out to F5; attacker drives F1 load to overwhelm the Control API and degrade lifecycle for all sessions. | M | H | High | Per-tenant MCP-calls/min quota at the Control API bounds the F1→F5 fan-in; MCP success ≥99.9% and control-plane RTO backstop availability; kill-switch path is independent of the MCP fan-in. | NFR-COST-06 + PERF-01 + REL-01 + SEC-01; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | Quota tuning and shared control-plane saturation are a config concern, not an architecture gap. | MITIGATED |
| P1-E1 | P1→F5 toward Control API | E | Compromised/malicious gateway tries to reach the kill-switch, denylist, or lifecycle mutation it must not hold. | L | H | High | Structural, not in-process: those routes live only on the operator-only ingress of a separate runnable unit, unreachable from the MCP surface by network policy; gateway holds no credential and no kill-switch, so full compromise yields no standing secret or lifecycle control; service identity bound to container_name. | NFR-SEC-26 + §3 split; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | No runtime gate proves the gateway service identity cannot reach the operator API (negative-reachability assertion absent). | MITIGATED |
| P1-E2 | F1 caller→gateway | E | Caller invokes a tool/session operation beyond its authorization because the gateway authenticates but does not enforce per-tool/per-action authz. | M | H | High | Audience-validated authN bounds who reaches the surface; gateway holds no credential and host-attested identity blocks cross-session addressing downstream — but fine-grained per-tool/action authz is not in canon. | none; NYDFS §500.7, DORA Art.6, EU AI Act Art.13/14, CCM IAM-08 | Per-tool/per-action authorization (G1/G8) uncaptured (#187). | OPEN |
| P2-S1 | P2 operator identity (F2/F3 terminate) | S | Adversary impersonates a PAM-JIT operator to reach lifecycle, quota, denylist, or the kill-switch across all tenants. | L | H | Med | Operator-only ingress on a distinct process; no shared service accounts; full shelf binds operator identity to a short-lived customer-IdP claim, minimal shelf to a host-rooted local credential; operator MFA at the IdP; identity never derived from a body claim. | NFR-COMP-29; NYDFS §500.7/§500.12, DORA Art.6/Art.28, EU AI Act Art.14, CCM IAM-06/08 | IdP or JIT-session compromise still yields operator authority, bounded by short TTL and audit; IdP compromise is customer-owned and out of OCU scope. | MITIGATED |
| P2-S2 | F5 gateway→Control API (terminates) | S | P1 (or a process that compromised P1) forges a service identity to invoke operator-only lifecycle/kill-switch routes, collapsing the agent/operator split. | L | H | Med | Two separate containers, operator-only ingress; MCP surface has no network route to kill-switch/denylist routes; P1→P2 token (≤60min) scoped to session create/status only; operator capabilities not on the service-identity audience. | NFR-SEC-09 + SEC-26 + §3 split; NYDFS §500.7, NIST 800-207 §2.1, CCM IAM-06 | Session-create scope reachable from P1 remains; bounded by quota (P2-D1). | MITIGATED |
| P2-T1 | F6 Control API→sandbox (P2 source) | T | In-sandbox root spoofs another session's container_name, replays a Session JWT, or dials the supervisor through its own stack to inject lifecycle commands. | M | H | High | Host dials guest, channel off any guest-reachable network (vsock / host-unix-socket; TCP listener rejects loopback/own-interface); caller identity host-attested; guest-supplied id is a hint cross-checked; Session JWT bound to container_name, ≤60min, rotated. | NFR-SEC-43; NYDFS §500.7, DORA Art.6, NIST 800-207 §2.1, CCM IAM-06 | JWT-at-rest on snapshot/hibernation is a separate hole (M-3/#184): a live token from a paused image is replayable within TTL. | MITIGATED |
| P2-T2 | F6 (denylist/kill-switch on RPC path) | T | P5 drops/stalls the host-dialed control RPC so a revoked or killed session keeps executing past SLA. | L | H | Med | Denylist checked host-side on every RPC (guest cannot read or mutate it); kill-switch is a host-initiated container stop ≤30s p99, not a cooperative guest action; fail-closed — channel unreachable yields no new authority. | NFR-SEC-01; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | Stop SLA assumes a trustworthy host clock for TTL/SLA accounting (#185). | PARTIAL |
| P2-R1 | P2 privileged operator actions | R | Operator (or a spoofed one) force-kills, edits the denylist, or overrides quota beyond tier-downgrade and denies it; only tier-downgrade is mandatorily audited (M-2). | M | H | High | Every operator action must emit a mandatory OCSF event into the hash-chained pipeline, independent of the operator's assertion; operator identity bound; the mandatorily-audited action set must be enumerated, not limited to tier-downgrade. | none; NYDFS §500.6, EU AI Act Art.12/Art.14, DORA Art.10, CCM LOG-01 | No NFR mandates audit for privileged operator actions beyond tier-downgrade (#186/M-2). | OPEN |
| P2-R2 | E4 SOAR (action attribution; via F4) | R | A SOAR revoke/kill is disputed (SOAR or a forged caller?), or replayed/forged to deny service. | L | M | Med | SOAR uses a signed admin API; the control plane verifies the signature against E4's key before acting, binding the action to SOAR; the action emits an OCSF event with the SOAR principal. | NFR-SEC-01; NYDFS §500.6, DORA Art.28, CCM IAM-08/LOG-01 | Mandatory-audit gap (#186/M-2) means a SOAR revoke of a non-tier action may not be guaranteed-audited; inherits P2-R1. | PARTIAL |
| P2-I1 | P2 control-plane metadata / session registry | I | Caller (via F5) or operator enumerates session registry, container_names, quota, or tenant ids of other tenants; metadata is itself sensitive. | L | M | Med | Service-identity audience scoped to the caller's own sessions; operator authority scoped per access-privilege model; control plane carries no customer payload; host-attested identity blocks cross-session F6 status queries. | NFR-IC-04; NIST 800-207 §2.1, NYDFS §500.7, DORA Art.28(2)(c), CCM IAM-06 | No-customer-payload gate not yet a measurable target (#149). | PARTIAL |
| P2-D1 | F1→F5 inbound + F2/F3/F4 operator ingress | D | Flood of session-create (via F5) or operator/SOAR calls exhausts the single control-plane instance, blocking the kill-switch/revoke path during an incident. | M | H | High | Per-caller quota at P2; operator ingress is a distinct path from the agent/service-identity path so agent-side flooding cannot starve the kill-switch route; fail-closed denies new sessions rather than degrading revoke. | NFR-REL-01; NYDFS §500.7, DORA Art.6, EU AI Act Art.14, CCM IAM-06 | Kill-switch ≤30s SLA under a concurrent control-plane DoS not separately guaranteed; path isolation asserted at zone level, not a measured revoke-availability NFR. | PARTIAL |
| P2-D2 | F3 IdP→Control API (terminates) | D | IdP unreachable blocks operator authN / new-session issuance, stranding the operator during an incident. | M | M | Med | Revoke and kill-switch run off the locally-held denylist, not a fresh IdP assertion; IdP unreachable → new sessions denied (fail-closed), in-flight sessions continue under existing token until TTL or revoke; the highest-value operator action survives an IdP outage. | NFR-SEC-01 + REL-01; NYDFS §500.12, DORA Art.6, EU AI Act Art.14, CCM IAM-08 | New privileged-session login during an IdP outage falls back to local credential only on the minimal shelf; full-shelf operators may be locked out of new logins until the IdP returns. | MITIGATED |
| P2-E1 | P2 agent path → operator capability | E | Agent-path principal (F1→F5, compromised P1, or a P5 that reached P1) escalates from metadata-only ops to operator capabilities by finding a route into the operator surface. | L | H | High | Kill-switch unreachable from the MCP path by deploy-time network policy across two separate containers, not an in-process guard; distinct privilege set and operator-only ingress; the P1 service-identity audience carries no operator scopes; no MCP-surface code path maps to a lifecycle/kill-switch route. | NFR-SEC-01; NIST 800-207 §2.1, NYDFS §500.7, EU AI Act Art.14, CCM IAM-06 | A misconfiguration co-locating the two surfaces defeats it; no IaC-policy NFR yet CI-verifies the two-container separation (#149). | PARTIAL |
| P2-E2 | F6 Control API→sandbox (host-attested) | E | P5 with in-sandbox root presents a victim session's container_name on F6 to receive its Session JWT or lifecycle authority on a shared-kernel tier. | M | H | High | Host derives caller identity itself (hypervisor CID / kernel peer-creds / un-enumerable socket path); guest-supplied id is a hint cross-checked; a guest with in-sandbox root cannot present another session's identity nor reach the channel via its own stack. | NFR-SEC-43; NIST 800-207 §2.1, NYDFS §500.7, DORA Art.6, CCM IAM-06 | On shared-kernel tiers, attribution is only as strong as the kernel boundary; hardware side-channel (#183) or kernel escape (bounded by NFR-SEC-02) could undermine per-session separation; cross-tenant on bare runc already forbidden. | MITIGATED |
| P3-S1 | P3 Credential custody (process) | S | Adversarial guest impersonates the egress edge to custody to pull a lease directly, or a co-located non-egress component forges the edge's workload identity. | L | H | Med | Custody exposes no guest-facing interface, reachable only host-side; lease is edge-pulled over host-side inter-component identity (host-local key minimal / customer-PKI full shelf); guest has no route to custody, its only path is the egress edge which terminates and re-originates; caller identity host-derived. | NFR-SEC-23; NYDFS §500.15(a), DORA Art.28, CCM CEK-08, NIST §3.2 | No transport reaches custody from the guest; residual is a misissued workload identity for a non-egress host component, bounded by per-component PKI scoping. | MITIGATED |
| P3-T1 | P3 custody — STS delegation | T | Tampering with delegated-STS issuance so custody mints a lease for the wrong resource, a wider scope, or a longer TTL; a compromised custody re-signs against an attacker policy. | L | H | Med | Host-side custody with delegated STS and rotation; lease TTL ≤15min, revoke ≤1min high-value; inter-component traffic encrypted; signing key host-local (minimal) or HSM-rooted FIPS 140-3 L3 (full shelf); lease-issue audited. | NFR-SEC-29; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | Per-resource scope bounded to one bucket prefix / API-key class, but minimum lease scope per tool/action unstated (see P3-E1). | PARTIAL |
| P3-R1 | P3 custody (process) | R | A privileged operator forces custody to mint/rotate a high-value lease and the action is not mandatorily recorded; operator repudiates the credential grant. | M | M | Med | Custody emits lease-issue/rotate events to the hash-chained pipeline; kill-switch/denylist path audited — but mandatory audit of privileged operator actions beyond tier-downgrade is not yet canon. | none; NYDFS §500.15(a), DORA Art.28, CCM CEK-08, NIST §3.2 | Forced mint, rotation override, scope change against custody not mandatorily audited (#186/M-2). | OPEN |
| P3-I1 | P3 custody / D1 store at rest | I | Real upstream credentials in D1 disclosed at rest (disk/snapshot) or via memory-scrape, giving standing upstream access independent of any session. | L | H | High | Guest holds no standing secret; real creds host-side with no guest-facing interface; at rest AES-256-GCM, key host-local (minimal) / HSM-rooted (full shelf), cryptographic erasure; delegated STS hands out short-lived leases, not the root, so a session-path compromise cannot reach the root. | NFR-SEC-33; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | At-rest disk closed; memory-scrape of the live custody process and host snapshot/hibernation escape the at-rest envelope (M-3/#184; NFR-SEC-32 disclaims in-memory secrecy) — see P3-I2. | PARTIAL |
| P3-I2 | P3 custody — snapshot/hibernation | I | A snapshot/hibernation image of the custody host (or a sandbox holding a fresh lease) captures a live lease or in-memory root signing material at rest. | M | H | High | TTL ≤15min and revoke ≤1min bound a captured lease's window; at-rest encryption covers the persistent store — neither covers a memory image taken while the key/lease is live in RAM. | none; NYDFS §500.15(a), DORA Art.28, CCM CEK-08 | Snapshot/hibernation holds a live lease at rest outside the at-rest envelope (M-3/#184). | OPEN |
| P3-D1 | P3 custody (process) | D | Custody made unavailable (exhaustion, crash, key-store lockout) so the edge cannot fetch leases and all upstream-authenticated traffic for active sessions fails. | L | M | Med | Egress fail-closed: no injection → the leg drops, not bypasses; in-flight sessions continue under existing leases until TTL; guest has no path to drive custody load, so DoS must originate host-side. | NFR-SEC-23; NYDFS §500.15(a), DORA Art.28, CCM CEK-08, NIST §3.2 | No stated RTO/RPO at this element; per-component host-side resource-exhaustion containment unspecified (overlaps #188). | PARTIAL |
| P3-E1 | P3 custody — lease scoping | E | A session obtains, via the edge, a lease broader than the tool/action requires (full-bucket vs prefix, write vs read), escalating from task scope to the full resource class. | M | H | High | Lease per-upstream-resource (one bucket prefix / API-key class), ≤15min TTL, ≤1min high-value revoke; delegated STS narrows below the root — but minimum lease scope per tool/action is not yet canon (scope bounded at resource-class, not action, granularity). | NFR-SEC-29; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | Per-tool/action authz and minimum lease scope unstated (#187/G1/G8); resource-class+TTL+revoke bounds blast radius but not least-privilege per action. | PARTIAL |
| P3-E2 | P3 custody — STS delegation | E | Confused-deputy: attacker tricks custody into delegating against a session identity that is not the requester's, minting a lease for a different session. | L | H | Med | The session identity custody binds to is host-attested, not guest-supplied; lease fetched by the edge bound to container_name; delegated STS is per-session, never reusable cross-session; lease-issue audited. | NFR-SEC-43; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | Cross-session confused-deputy blocked; residual is trusted-clock dependence of TTL/revoke (see P3-T2). | MITIGATED |
| P3-T2 | P3 custody — lease TTL enforcement | T | Clock rollback on the custody/egress host extends a lease past its ≤15min TTL or defers the ≤1min revoke window, so a credential the architecture believes expired stays usable. | L | H | Med | TTL and revoke latencies stated; revoke also propagates via the platform denylist independent of self-expiry — but all TTL/expiry enforcement assumes a trustworthy clock; no invariant pins trusted time. | none; NYDFS §500.15(a), DORA Art.28, CCM CEK-08 | Trusted-time / clock-rollback unaddressed (#185). | OPEN |
| F8-T1 | F8 custody→egress edge | T | Lease altered in transit between custody and the edge — scope widened, TTL extended, rebound to another destination — by a host-side MITM. | L | H | Med | F8 is host-side, edge-initiated, encrypted (TLS 1.3); lease short-lived and carries STS-signed scope so tampering must defeat the signer (host-local / HSM-rooted); flow not guest-reachable. | NFR-SEC-33; NYDFS §500.15(a), DORA Art.28, CCM CEK-08 | In-transit integrity covered; what the signed lease grants is the open question (P3-E1, P3-T2). | MITIGATED |
| F8-I1 | F8 custody→egress edge | I | The scoped lease leaks on F8 — observed on the wire or captured at the edge — and is replayed against the upstream within TTL. | L | H | Med | Lease fetched at injection, never persisted to the guest, host-side encrypted; scoped (one resource class) and short-lived (≤15min, ≤1min high-value revoke); edge injects only in MITM mode for the matching destination; revoked session blocks injection. | NFR-SEC-29; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | Bounded by TTL/scope/revoke; a host-memory-snapshot capture escapes the at-rest envelope (P3-I2/#184); PoP/client-mTLS upstreams (#176) shift where the credential lives. | PARTIAL |
| P4-S1 | F7 broker→sandbox (mount, filesystem_id) | S | In-sandbox root presents another session's filesystem_id (or forges a body id) so the broker serves a different session's prefix — cross-tenant via handle confusion. | M | H | High | F7 host-dials-guest; broker binds each mount to a host-attested session identity and treats a guest-supplied filesystem_id as a hint cross-checked, never authority; handle is session-scoped and selects only that session's prefix; backend cred and prefix-scoping host-side. | NFR-SEC-43 + SEC-25; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.10, CCM CEK-08/DSP-01 | A broker-side authz bug binding the wrong prefix; covered by per-tier zero red-team pass, not the invariant text. | MITIGATED |
| P4-S2 | P4 broker→backend | S | Compromised/impersonated broker authenticates to the backend as the tenant principal and reaches objects outside any live session scope. | L | H | Med | Full shelf gives the broker a customer-PKI workload identity with STS-scoped-per-session backend creds, so it can assume only the narrow per-session role; minimal shelf falls back to a host-local backend credential. | NFR-SEC-25; NYDFS §500.15(a), DORA Art.28, CCM CEK-08 | Minimal shelf host-local credential is broader than per-session STS; blast radius is the whole bucket on broker-host compromise — accepted for one-click solo, called out by the shelf split. | PARTIAL |
| P4-T1 | F7 broker→sandbox (mount) | T | Guest crafts malformed/oversized file-ops (path traversal, symlink/`..` escape, oversized write) to write outside the session prefix or corrupt another mount. | M | H | High | Broker exposes only file-op verbs, not the object-store protocol; resolves every path inside the host-attested prefix and rejects traversal; STS credential is prefix-scoped, so even a resolver bug cannot sign a write outside the prefix at the backend. | NFR-SEC-25; EU AI Act Art.10, CCM DSP-01, DORA Art.28 | Path-confinement is an impl property (traversal-bug residual, caught by property-tests); the oversized-write tail is unbounded — per-sandbox disk quota / deterministic OOM uncaptured (#188). | PARTIAL |
| P4-T2 | F10 broker→edge→backend | T | Broker-signed backend request rewritten in flight at the edge, or a middlebox alters the payload between broker and backend. | L | M | Low | Broker signs once; F10 traverses the edge allow-list-only with no TLS termination, so no middlebox rewrites the signature; strict upstream TLS 1.3 protects the leg; inspection, where required, is at the broker on plaintext before signing. | NFR-SEC-05 + SEC-33; NYDFS §500.15(a), DORA Art.28, CCM CEK-08 | Backend-side replay-window handling is the backend's, outside OCU. | MITIGATED |
| P4-R1 | P4 broker (action attribution; via F11) | R | Storage read/write and mount-grant actions are not attributable after the fact (which session touched which prefix), so cross-tenant access cannot be proven or disproven. | M | M | Med | Broker is one of six OCSF sources feeding the hash-chained pipeline; each file-op carries the host-attested session identity, durable store with daily Merkle head; backend request session-scoped. | NFR-SEC-43 + SEC-03 + SEC-25; NYDFS §500.6, EU AI Act Art.12, CCM LOG-01/02 | Timestamp integrity under clock-rollback is the separate trusted-time hole (#185), cross-referenced not double-counted. | MITIGATED |
| P4-I1 | P4 broker — backend cred in process | I | Disclosure of the backend credential the broker holds (process compromise, memory scrape, leak onto the mount surface) gives direct backend access bypassing session scoping. | L | H | Med | Credential host-side only; guest holds a filesystem_id, never the backend key; broker exposes no object-store protocol toward the guest; full shelf narrows the held secret to an STS-scoped-per-session credential. | NFR-SEC-25 + SEC-33; NYDFS §500.15(a), CCM CEK-08, DORA Art.28 | Minimal shelf holds a long-lived host-local credential (full-backend on broker-host compromise); STS floor and per-action minimum unstated (#187). | PARTIAL |
| P4-I2 | F7 broker→sandbox (mount content) | I | Guest reads another tenant's user-data via cached/leftover content on a reused mount substrate (remanence), or a list enumerates beyond the prefix. | L | H | Med | Mount and handle session-scoped and bound to host-attested identity; user-data on a per-session volume, image carries no PII, cryptographic erasure on teardown; list/read confined to the session prefix. | NFR-SEC-15 + SEC-33 + SEC-25; EU AI Act Art.10, CCM DSP-01, NYDFS §500.15(a) | Cross-session remanence depends on broker teardown discipline; deterministic per-session erasure ordering not yet pinned to an NFR scenario. | PARTIAL |
| P4-D1 | F7 broker→sandbox (mount) | D | Hostile guest issues unbounded file-ops / huge writes / fd exhaustion against the mount, degrading the broker for co-located sessions (noisy-neighbour via shared broker). | M | M | Med | No standing canon control bounds broker-side per-session file-op rate, in-flight bytes, or fd count; the broker may be one-per-host serving many sessions, making it a shared DoS surface. | none; DORA Art.28, CCM IVS-06, NYDFS §500.7 | Per-sandbox resource-exhaustion containment (#188) does not extend to the broker file-op path; per-session broker rate/quota unstated. | OPEN |
| P4-D2 | F10 broker→edge→backend | D | Guest drives backend-bound traffic (large/repeated reads/writes) to exhaust per-session STS quota or trigger backend throttling/cost-blowout. | L | M | Low | F10 traverses the single egress allow-list as one destination, so the path is observable and policy-gated; a direct second broker→backend dial is forbidden so all backend traffic is visible to one control. | NFR-SEC-16 + SEC-27; DORA Art.28, CCM IVS-09, NYDFS §500.5 | Per-session backend rate/quota ceiling not pinned (overlaps #188); egress sees the leg but no canon limit caps backend cost per session. | PARTIAL |
| P4-E1 | P4 broker — confused-deputy via mount | E | Guest induces the higher-privileged broker to perform a backend op it could not authorize itself, e.g. smuggling a backend key/prefix through a file-op argument the broker forwards into a signed request. | M | H | High | Privilege separation: guest names only file-op verbs against its session-scoped handle; broker maps that to a request signed with its own credential, and the handle is a hint cross-checked against host-attested identity; the guest cannot name a backend object directly. | NFR-SEC-25 + SEC-43; NIST 800-207 §2.1, DORA Art.28, CCM CEK-08/DSP-01 | The deputy boundary is only as tight as the verb-to-backend mapping; per-action authz and minimum lease scope unstated (#187). | PARTIAL |
| P4-E2 | F10 broker→egress (second outbound path) | E | Broker opens a direct broker-to-object-store dial bypassing the edge, creating an outbound path the single egress control cannot see — an uncontrolled exfil channel. | L | H | Med | A direct broker-to-backend dial bypassing the edge is forbidden; the backend leg must traverse the single egress as one allow-list destination, so no second outbound path exists by policy. | NFR-SEC-16 + SEC-27; CCM IVS-09/DSP-05, DORA Art.30 | Broker-served file content is inspected only at the broker on plaintext; transparent-mode egress is content-blind to the F10 leg (no TLS termination), so agentic exfil through legitimately-written objects needs broker-side DLP (#182). | PARTIAL |
| P5-E1 | P5 runtime → host kernel | E | Adversarial in-sandbox root exploits a runtime/kernel boundary to escape and gain host-kernel execution, reaching co-tenant sandboxes, broker, custody, or control plane. | M | H | High | Tier-appropriate escape resistance: gVisor user-space kernel v1 default, microVM hardware-virt post-v1, multi-tenant agent-execution forbidden on bare runc; per tier seccomp-BPF, Landlock, cap-drop ALL, no-new-privileges, userns, read-only rootfs, no docker.sock, cgroup limits; zero red-team escape pass per release per tier. | NFR-SEC-02 + SEC-14; EU AI Act Art.15(4), CCM IVS-06/09, NYDFS §500.7/§500.15 | Zero-day in the chosen kernel/sentry/hypervisor; bare-runc solo default has only shared-kernel hardening, acceptable only under the trusted_operator single-tenant profile. | MITIGATED |
| P5-S1 | F6 Control API→P5 (P5 adversarial view) | S | Compromised guest forges/replays another session's identity to the host — body-supplied id/JWT or dialing the supervisor through its own stack — to act as another tenant or reach the kill-switch. | M | H | High | Host-attested caller identity (hypervisor CID / kernel peer-creds / un-enumerable socket path); guest-supplied id is a hint cross-checked; control/exec channel off any guest-reachable network (host dials, guest listens; TCP listener rejects loopback/own-interface); kill-switch unreachable from the guest path. | NFR-SEC-43; NYDFS §500.7, CCM IAM-06, EU AI Act Art.14 | Post-escape (P5-E1) the host-attestation root itself can be subverted; identity holds only while the runtime boundary holds. | MITIGATED |
| P5-T1 | F11 P5→audit (guest as its own source) | T | With in-sandbox root the guest suppresses, fabricates, reorders, or backfills the OCSF events it emits for its own in-sandbox actions; the chain protects events once in the pipeline, not their truthfulness at origin. | H | H | High | Out-of-band evidence for in-sandbox actions is not yet specified: need host-/hypervisor-side capture (egress event correlation, sentry/VMM syscall telemetry, broker file-op log) the guest cannot author; the chain secures transport+store integrity, not source veracity. | none; EU AI Act Art.12, NYDFS §500.6, DORA Art.10 | In-sandbox action evidence is attacker-authorable; no canon NFR for out-of-band capture (M-4/#181). | OPEN |
| P5-I1 | P5 cross-tenant on shared substrate | I | Guest mounts a microarchitectural side-channel (Spectre/MDS, cache timing, port contention) against a co-resident tenant or the host, independent of any logical escape. | M | H | High | No canon NFR; candidate controls (core-scheduling/sibling-pinning, microcode flags, dedicated node pool, microVM tier) are tracked but unstated as a measurable target; higher tiers reduce co-residency but do not bound the channel. | none; EU AI Act Art.15(4), CCM IVS-06, NYDFS §500.15 | Cross-tenant hardware side-channel, no measurable "tenant A cannot observe tenant B" target (#183/#148). | OPEN |
| P5-I2 | P5 state at rest (snapshot/hibernation) | I | Snapshot/hibernation persists guest memory/disk holding a live Session JWT and mount handle at rest; an image read recovers a usable token before TTL expiry. | M | H | High | At-rest encryption (AES-256-GCM, cryptographic erasure) covers the volume but not a live token captured in a snapshot taken while the session holds it; no control invalidates the token on snapshot or excludes it from the image; short JWT TTL (≤60min) caps but does not eliminate the window. | none; NYDFS §500.15(a), CCM CEK-08, EU AI Act Art.15 | Snapshot/hibernation holds a live session token at rest; no invalidate-on-snapshot or token-exclusion control (M-3/#184). | OPEN |
| P5-D1 | P5 resource consumption on shared substrate | D | Guest exhausts node resources (fork-bomb/PID explosion, disk fill, memory pressure → non-deterministic OOM) to degrade or kill co-resident sessions or the host. | H | M | High | cgroup limits bound CPU/memory at the container level, but per-sandbox PID ceiling, disk quota on mount/scratch, and deterministic OOM-kill scoping are not specified as enforced containment targets. | NFR-SEC-14; EU AI Act Art.15(4), CCM IVS-09, DORA Art.6 | PID ceiling, disk quota, deterministic OOM scoping unstated (#188). | PARTIAL |
| P5-D2 | F9 P5→egress edge (sole outbound) | D | Guest floods the edge with connection/request volume to deny outbound access to itself and co-tenant sessions sharing the edge, or to amplify against an allow-listed upstream. | M | M | Med | Single forward proxy, deny-by-default allow-list, bounds reachable destinations; fail-closed drops traffic if the proxy is unreachable rather than bypassing; per-session egress rate/connection quota is a component-spec target, not a canon NFR. | NFR-SEC-05 + SEC-27; EU AI Act Art.15(4), CCM IVS-09, NYDFS §500.5 | Destination set bounded; per-session egress rate-limit not stated (folds into #188). | PARTIAL |
| P5-I3 | F9 P5→edge→upstream (transparent mode) | I | Agentic exfiltration: guest tunnels tenant data to an allow-listed destination over default transparent (content-blind) egress (DNS/HTTP smuggling, encoded payloads), undetectable because inspection exists only in opt-in MITM. | H | H | High | Allow-list + structured deny bounds destinations but not payload content; DLP-ICAP inspection runs only in opt-in MITM mode; default transparent mode is content-blind by design; AI-content/exfil policy is the customer AI-gateway's contract on this path. | none; CCM DSP-05, EU AI Act Art.15, NYDFS §500.15 | Default egress content-blind; agentic exfil undetectable in transparent mode (#182). | OPEN |
| P5-R1 | P5 in-sandbox action accountability | R | An in-sandbox action cannot be non-repudiably attributed because its only authoritative record originates inside the guest; with P5-T1 the session can deny/misattribute, and a trusted-time dependency lets ordering be questioned. | M | H | High | Host-attested session identity binds host-observed events; hash-chained audit orders accepted events. Gaps: in-sandbox evidence is guest-authored (P5-T1/#181) and ordering assumes a trustworthy clock (no trusted-time control). | NFR-SEC-43 + SEC-03; NYDFS §500.6, EU AI Act Art.12, DORA Art.10 | Non-repudiation of in-sandbox actions depends on guest-authored evidence (M-4/#181) and a trusted clock (#185). | OPEN |
| P5-S2 | F7 broker→P5 (P5 adversarial view) | S | Compromised guest presents another session's filesystem_id (or a guessed handle) to the broker to operate on a different tenant's user-data. | M | H | High | Broker cross-checks the guest-supplied filesystem_id against host-attested per-session identity; handle is session-scoped and carries no backend credential; broker signs its own requests so a stolen handle cannot mint backend access; host dials guest over the off-network channel. | NFR-SEC-43 + SEC-25; NYDFS §500.15(a), CCM CEK-08/DSP-01, EU AI Act Art.10 | Holds while the runtime/attestation boundary holds; verify filesystem_id→host-identity binding in the broker component spec. | MITIGATED |
| P5-T2 | P5 standing-secret harvest | T | Guest with in-sandbox root searches memory, filesystem, and env for a standing upstream secret to reuse outside policy. | H | H | High | Guest holds no standing secret by design: upstream auth injected at the edge, never in the guest; custody host-side with delegated STS and no guest-facing interface; guest holds only session-scoped Session JWT + filesystem_id; lease ≤15min, revoke ≤1min high-value. | NFR-SEC-23 + SEC-29 + SEC-25; NYDFS §500.15(a), CCM CEK-08, DORA Art.28 | The session-scoped tokens the guest does hold are harvestable in-guest (feeds P5-I2, P5-S1); blast radius bounded to one session by short TTL. | MITIGATED |
| P6-S1 | P6 guest-facing listener on F9 | S | Adversarial guest forges a peer identity to the edge to obtain another session's injected upstream credential by binding its request to a victim session_id. | M | H | High | Edge derives requesting-session identity from the host-attested network-bound origin of the inbound leg, not a guest-asserted session_id; egress is network-bound (a sandbox has no route out except its own edge attachment), so the edge maps request→session by un-spoofable origin and pulls only that session's lease. | NFR-SEC-27 + SEC-43; NIST PEP §3.4.1, NYDFS §500.5, DORA Art.30, EU AI Act Art.14, CCM IVS-09 | Strength of network-origin→session binding on a shared host; if sandboxes share one edge namespace the attribution weakens (tracked under NFR-SEC-43; per-sandbox origin isolation assumed). | MITIGATED |
| P6-S2 | Edge→upstream (MITM mode) | S | Upstream impersonation: edge connects to an attacker-controlled host masquerading as the allow-listed upstream (DNS spoof / BGP / compromised resolver) and injects the real credential into the impostor. | L | H | Med | Edge enforces strict upstream TLS verification, fail-closed on validation failure; upstream identity validated against the allow-list entry, not just reachability; injection only after the upstream TLS validates. | NFR-SEC-05 + SEC-33; NIST implicit→untrusted, NYDFS §500.15, DORA Art.28, EU AI Act Art.15, CCM IVS-09 | Cert-pinning / client-mTLS / DPoP upstreams cannot be satisfied by edge injection (#176); for those the edge cannot both validate and inject — unsupported in MITM-inject mode rather than spoofable. | MITIGATED |
| P6-T1 | Edge→upstream (MITM-inspecting) | T | In MITM mode the edge holds plaintext between decrypt and re-encrypt; a compromised/misconfigured edge alters request/response bodies undetected, as the edge is the trusted re-signer. | L | H | Med | MITM is opt-in per-destination, customer-CA-rooted, TLS 1.3 both legs; the plaintext carve-out is the only inspection point and is auditable; edge-process integrity is a Compute/host-hardening property, not guest-reachable. | NFR-SEC-05 + SEC-33; NIST PEP §3.4.1, NYDFS §500.15, DORA Art.30, EU AI Act Art.14, CCM DSP-05 | Edge-process integrity (signed binary, attested config) is not asserted by a listed NFR; a compromised MITM edge has no in-band detection — needs edge-binary/config attestation (Open Question, new issue). | PARTIAL |
| P6-T2 | F10 broker-signed leg through edge | T | Edge tampers with or re-signs the broker's pre-signed backend request as it traverses the allow-list-only leg, altering the object-store operation. | L | M | Low | Broker signs once at the broker; the edge traverses this leg allow-list-only with no TLS termination, so the signature survives and nothing downstream rewrites it; an altered request fails the broker's signature at the backend. | NFR-SEC-25 + SEC-16; NIST custody-of-backend-cred, NYDFS §500.15, DORA Art.28, EU AI Act Art.10, CCM CEK-08/DSP-01 | Source-side signature custody is owned by P4; the only added property here is no-TLS-termination on the broker leg, which holds by construction. | MITIGATED |
| P6-R1 | P6→audit (F11) and F9 deny path | R | An exfiltration or policy-violating egress attempt is later disputed because the decision (allow/deny, destination, injected-or-not, session) was not durably recorded, or the deny reason was free-text. | M | M | Med | Edge emits OCSF for every allow and deny; denials carry a structured `x-deny-reason` header so audit and SOAR classify without parsing free text; SNI pre-filter drops and L7 inspections both record. | NFR-SEC-03; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01 | In transparent pass-through the edge logs destination (SNI/connection metadata) but not request content, so a content-level dispute is non-repudiable only by metadata — same root cause as #182. | PARTIAL |
| P6-I1 | F9 sandbox→edge (content-blind transparent) | I | PRIMARY ADVERSARY exfil: hostile guest tunnels sensitive data to an allow-listed-but-attacker-usable destination; transparent pass-through is content-blind, so agentic exfil to an allowed endpoint is undetectable. | H | H | High | Architecture caps blast radius (deny-by-default allow-list narrows destinations; guest holds no standing secret so it can exfil only what it reads in-session); content inspection/DLP requires opt-in per-destination MITM mode; default transparent mode does not inspect. | none; NIST PEP §3.4.1, NYDFS §500.5, DORA Art.30, EU AI Act Art.14, CCM DSP-05 | Default egress content-blind; agentic exfil to an allow-listed destination undetectable; an allowed LLM/API endpoint is a viable exfil channel (#182). | OPEN |
| P6-I2 | F8 custody→edge lease + injected cred in flight | I | The lease / injected upstream credential leaks toward the guest — an error/redirect or reflected header carries the injected Authorization back into a guest-visible response in MITM mode. | M | H | High | Credential injected only on the edge-originated upstream leg, never on the guest→edge leg; guest sends an unauthenticated request; edge must strip injected auth from any response surface; lease short-TTL (≤15min) and per-resource scoped. | NFR-SEC-23 + SEC-29; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | No listed NFR mandates response-side stripping of reflected injected auth at the edge — needs an explicit no-credential-in-response invariant (Open Question; ties to #176). | PARTIAL |
| P6-I3 | Edge inspection point (MITM plaintext carve-out) | I | The decrypt/re-encrypt segment exposes plaintext of upstream traffic at the edge; a compromised edge or its memory/logs leak that plaintext. | L | H | Med | MITM opt-in per-destination; the plaintext carve-out is a single named segment, customer-CA-rooted, TLS 1.3 both legs; transparent mode never terminates TLS; the broker leg does not terminate, so the edge sees only ciphertext there. | NFR-SEC-05 + SEC-33; NYDFS §500.15, DORA Art.30, EU AI Act Art.14, CCM DSP-05 | Plaintext-at-rest in edge memory/swap has no zeroization NFR (Open Question for memory-handling at the MITM segment). | PARTIAL |
| P6-D1 | F9 sandbox→edge (sole outbound); edge availability | D | Edge becomes unreachable (crash, flood, upstream timeout storm); as the sole outbound path its loss blocks all session egress; the variant is fail-open vs fail-closed. | M | H | High | Fail-closed by design: if the proxy is unreachable the Compute plane drops outbound traffic and never bypasses; unallowed destinations dropped cheaply at SNI pre-filter before TLS, limiting handshake-exhaustion cost from a flooding guest. | NFR-SEC-05 + SEC-27; NIST PEP §3.4.1, NYDFS §500.5, DORA Art.30, EU AI Act Art.14, CCM IVS-09 | No NFR bounds per-sandbox egress connection-rate / FD exhaustion against the shared edge; one hostile guest could degrade the sole outbound path for co-tenant sessions (#188). | OPEN |
| P6-D2 | F8 custody→edge (lease fetch at injection) | D | Custody unreachable at injection time stalls every injection-needing egress leg, or the edge fails open and forwards without the lease / after revoke. | L | M | Med | Revoke acts at the edge by withholding injection; fail-closed — no lease means no injected leg, not an unauthenticated forward to an auth-requiring upstream; denylist propagates ≤5min, lease self-expires ≤15min. | NFR-SEC-29 + SEC-04 + SEC-01; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.14, CCM CEK-08 | Bounded by fail-closed; residual is the latency window between revoke and ≤5min denylist propagation, accepted by NFR-SEC-04. | MITIGATED |
| P6-D3 | Trusted-time dependency of lease TTL / denylist at edge | D | Clock rollback at the edge extends an expired lease past ≤15min TTL or defeats denylist-propagation timing, so a revoked session keeps an injected credential alive. | L | M | Med | TTL and revoke timing assume a trustworthy clock; the denylist is also checked directly (not purely TTL-driven), giving a non-clock revoke path. | none; NIST PEP §3.4.1, NYDFS §500.5, DORA Art.30, EU AI Act Art.14, CCM IVS-09 | No trusted-time / anti-rollback control; TTL-only expiry remains clock-dependent (#185). | OPEN |
| P6-E1 | F9 sandbox→edge (allow-list bypass) | E | PRIMARY ADVERSARY reaches a destination outside the allow-list via domain-fronting (SNI≠Host), CONNECT abuse, DNS rebinding, raw-IP/non-HTTP, or a second route bypassing the edge. | H | H | High | Deny-by-default allow-list is the only outbound path; sandbox has no route out except the edge (no second NIC/route); unallowed destinations dropped at SNI pre-filter before TLS; allowed destinations inspected at L7 so SNI/Host mismatch is caught on the inspected leg; broker direct-dial bypass forbidden. | NFR-SEC-27 + SEC-05 + SEC-16; NIST PEP §3.4.1, NYDFS §500.5, DORA Art.30, EU AI Act Art.14, CCM IVS-09 | SNI pre-filter alone misses SNI/Host domain-fronting on the transparent (non-inspected) leg; L7 SNI/Host consistency is enforced only where inspection runs (#182) — needs SNI/Host consistency enforcement in transparent mode (Open Question). | PARTIAL |
| P6-E2 | F9/upstream (forcing injection onto unintended destination) | E | Guest induces the edge to inject a high-value credential toward a destination it was not scoped for (cross-scope allow-list entry, open-redirect on an allowed upstream), elevating from session scope to the credential's full upstream authority. | M | H | High | Custody lease is per-upstream-resource scoped (one bucket prefix / API-key class) and short-TTL; the edge pulls only the lease matching the validated destination, so an injected credential is bounded to its resource scope even if mis-routed. | NFR-SEC-29 + SEC-23; NYDFS §500.15(a), DORA Art.28, EU AI Act Art.15, CCM CEK-08 | NFR-SEC-29 caps blast radius; the destination-to-lease binding rule (select/inject by validated post-redirect L7 destination, refuse redirect-following outside lease scope) and minimum-scope/action-level authz are uncaptured (#187/G1/G8). | PARTIAL |
| P6-E3 | F9 destinations requiring client-mTLS / cert-pinning / DPoP | E | An upstream that cannot accept edge-injected authorization forces a choice — place the credential in the guest (defeating no-standing-secret) or do not serve it; a guest-held workaround is itself an escalation. | M | H | High | Architecture does not permit a guest-held credential workaround; such upstreams are out of the edge-injection contract rather than served by placing keys in the guest. | none; NYDFS §500.15, DORA Art.28, EU AI Act Art.15, CCM IVS-09 | No NFR defines supported handling (broker-style host-side signer per scheme vs declared-unsupported) (#176). | OPEN |
| P7-S1 | P7 audit (OCSF source identity; via F11 fan-in) | S | Compromised P5 forges OCSF events impersonating another source container (source=control-plane/egress) so its actions are attributed to a trusted host-side component, or fabricates clean events to mask malicious tool-calls. | M | H | High | Source identity at fan-in should be the host-attested workload identity of the connecting principal, never a guest-settable source field; the guest channel is pinned to source=sandbox/<its-session>, and an event claiming any other source from that principal is rejected — the natural extension of the guest-supplied-identity-never-authoritative principle, but not yet canon. | none; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | No listed NFR binds the OCSF source field to a host-attested emitter at ingestion (M-4/#181 seam); content-trust deferred to P7-R2. | OPEN |
| P7-R1 | D2 audit store (log) | R | An adversary denies an action, or an audited party claims the log was altered/inserted after the fact, defeating non-repudiation. | L | H | Med | Hash-chained append-only store (each record links the prior hash, so deletion/reordering/insertion breaks the chain); daily Merkle head to a customer-chosen transparency log (operator-signed); HSM-signed envelope when customer KMS is wired; retention floor machine-enforced (7y default / 10y configurable). | NFR-SEC-03; NYDFS §500.6/§500.13, DORA Art.10, EU AI Act Art.12/Art.19(1), CCM LOG-01/02 | The chain proves no tampering after admission, not content truth at emission (P7-R2), and assumes a trustworthy clock for the daily-head cadence (P7-T2). | MITIGATED |
| P7-R2 | P5 sandbox (self-reported events; via F11) | R | The guest is its own audit source for purely in-sandbox actions (file edits, sub-process spawns, reasoning steps); with in-sandbox root it suppresses/omits/falsifies these before fan-in, so the chain faithfully records a lie. | H | H | High | Out-of-band evidence: host-observed signals the guest cannot edit — egress request log (P6), storage file-op log (P4), control-plane RPC log, runtime syscall/exec capture at the supervisor (gVisor sentry / microVM); the self-report is corroborated against these rather than trusted alone. No NFR yet mandates the out-of-band corroboration set. | none; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | Guest-as-its-own-audit-source needs a mandated out-of-band evidence set; purely in-sandbox actions with no host-side side-effect are unverifiable (M-4/#181). | OPEN |
| P7-T1 | F11 {6 sources}→audit (fan-in transport) | T | An on-path adversary or compromised intermediary alters/drops OCSF events in transit before they are hash-chained, so tamper-evidence never sees the modification. | L | M | Med | Inter-zone fan-in transport encrypted (TLS 1.3) under the cross-component encryption invariant; durable bus on the critical path commits events before acknowledgement; the chain is computed at the durable-bus ingest boundary, so admitted events are immediately tamper-evident. | NFR-SEC-33; NYDFS §500.6/§500.15, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | The emission-to-ingest-commit gap is the residual window, narrowed by source authentication (P7-S1) and out-of-band corroboration (P7-R2). | MITIGATED |
| P7-T2 | Audit pipeline (hash-chain / Merkle cadence) | T | Clock rollback / untrusted time backdates events, manipulates TTL-derived ordering, or stalls/forges the daily Merkle-head cadence so a tampered batch falls within a legitimate signing window. | M | H | High | Trusted-time source independent of any guest- or component-settable clock (monotonic chain-position sequence numbers alongside wall-clock; external trusted-time anchoring of the Merkle head via the transparency log's own timestamp). No NFR currently mandates a trusted-time / anti-rollback control. | none; NYDFS §500.6/§500.13, DORA Art.10, EU AI Act Art.12/Art.19(1), CCM LOG-01/02 | Until a trusted-time anchor is mandated, retention floors and ordering claims rest on an unverified clock (#185). | OPEN |
| P7-I1 | F11 audit→customer sink (OCSF bridge, outbound) | I | Audit events aggregate cross-tenant, high-classification content; the outbound bridge or the at-rest store leaks this aggregate to an unauthorized reader or wrong-tenant sink. | L | H | Med | Bridge transport TLS 1.3; at-rest store authenticated AES-256-GCM with cryptographic erasure; per-tenant residency enforced at pipeline routing so a tenant's events route only to that tenant's sink; the sink is a customer-owned external actor under a documented contract. | NFR-SEC-33; NYDFS §500.15, DORA Art.10, EU AI Act Art.12, CCM LOG-04 | Correct per-tenant sink routing depends on residency-routing config — a deployment-time control, not a runtime invariant. | MITIGATED |
| P7-D1 | F11 audit→customer sink (backpressure) | D | The downstream SIEM sink slows or becomes unreachable; backpressure fills the durable bus, forcing either event drops (audit gap) or source-container blocking (cascading session stall), amplifiable by a high-volume guest. | M | H | High | Durable bus on the critical path (events committed locally before acknowledgement) plus a local file-system sink as the always-present floor; the SIEM bridge is decoupled and replays from the durable store on recovery. NFR-REL-12 mandates the bus but no measurable end-to-end backpressure target. | NFR-REL-12; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | Sustained sink failure plus adversarial amplification has no defined saturation/spill behaviour or measurable target (#150; resource-exhaustion #188). | PARTIAL |
| P7-D2 | F11 sandbox→audit (event-volume flood) | D | A compromised guest emits a high-rate flood of well-formed OCSF events to exhaust collector ingest, fill the durable store / retention budget, or dilute true events with noise (distinct from P7-D1). | M | M | Med | Audit-pipeline backpressure headroom ≥10× peak with no silent drop and zero chain breaks bounds collector-ingest exhaustion and the no-drop dimension; per-source ingest fairness keyed to host-attested source identity would bound a single session's share but is not yet mandated. | NFR-PERF-10; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | No per-source/per-session fairness or retention-budget cap keyed to host-attested identity; forensic-value dilution within the no-drop budget uncovered (#188, extended to audit-ingest quota). | PARTIAL |
| P7-R3 | E2 operator · E4 SOAR (privileged-action attribution; via F2/F4) | R | A privileged operator acts beyond tier-downgrade (credential-store access, retention-policy change, denylist edit) without a mandatory audit event, so the action is repudiable and the trail has a privileged-action gap. | M | H | High | Every privileged operator/SOAR action should emit a mandatory audit event into the hash-chained store under host-attested operator identity (PAM-JIT, no shared service accounts). Present canon mandates audit only for tier-downgrade. | none; NYDFS §500.6/§500.7, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | Privileged operator actions beyond tier-downgrade not mandatorily audited (#186/M-2). | OPEN |
| P7-E2 | Audit pipeline (process) — elevation via collector | E | The collector parses guest-influenced OCSF content; a malformed/oversized event triggers a parser bug (memory corruption, injection into the downstream SIEM via unsanitized fields), elevating the adversary into code execution in the audit zone. | L | H | Med | Audit pipeline runs as its own hardened zone (no-new-privileges, cap-drop, syscall-confined per the hardening baseline); OCSF is a strict Published Language validated/normalized at the fan-in boundary; downstream SIEM fields are schema-bound, not free-text passthrough; the parser is exercised by property-based tests per the CI gate. | NFR-SEC-14; NYDFS §500.6, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | Standard parser-bug exposure to adversarial input, mitigated procedurally by the property-based-test CI gate rather than an architectural invariant. | MITIGATED |
| P7-T3 | D2 snapshot/hibernation of audit/session state at rest | T | A snapshot/hibernation image captured for the audit/forensic trail holds a live session token at rest; an adversary with image access replays the token to resume/impersonate a session and tampers with state outside the chain's view. | L | H | Med | At-rest encryption (AES-256-GCM) + cryptographic erasure on the image volume; session tokens excluded from snapshot scope. No NFR currently prevents a live session token from being captured in a snapshot/hibernation image at rest. | none; NYDFS §500.6/§500.15, DORA Art.10, EU AI Act Art.12, CCM LOG-01/02 | Snapshot/hibernation image holds a live session token at rest (M-3/#184). | OPEN |

## 4. Residual risks and accepted exposures

The OPEN-status rows carry no current canon control. See [`diagrams/06-threat-model.mmd`](diagrams/06-threat-model.mmd).

| ID | Residual exposure | Tracking issue |
|---|---|---|
| P1-I1 | Error-surface minimization and the no-customer-payload-on-control-plane gate are not yet measurable. | [#149](https://github.com/Wide-Moat/open-computer-use/issues/149) |
| P1-E2 | Per-tool / per-action authorization at the gateway (G1/G8) unstated. | [#187](https://github.com/Wide-Moat/open-computer-use/issues/187) |
| P2-R1 | Privileged operator actions beyond tier-downgrade not mandatorily audited (M-2). | [#186](https://github.com/Wide-Moat/open-computer-use/issues/186) |
| P3-R1 | Forced mint / rotation override / scope change against custody not mandatorily audited (M-2). | [#186](https://github.com/Wide-Moat/open-computer-use/issues/186) |
| P3-I2 | Snapshot/hibernation holds a live lease or in-memory root signing material at rest (M-3). | [#184](https://github.com/Wide-Moat/open-computer-use/issues/184) |
| P3-T2 | Lease TTL/revoke enforcement assumes a trustworthy clock. | [#185](https://github.com/Wide-Moat/open-computer-use/issues/185) |
| P4-D1 | Per-session broker file-op rate/quota and fd containment unstated. | [#188](https://github.com/Wide-Moat/open-computer-use/issues/188) |
| P5-T1 | In-sandbox action evidence is attacker-authorable; no out-of-band capture (M-4). | [#181](https://github.com/Wide-Moat/open-computer-use/issues/181) |
| P5-I1 | Cross-tenant hardware side-channel; no measurable isolation target. | [#183](https://github.com/Wide-Moat/open-computer-use/issues/183) |
| P5-I2 | Snapshot/hibernation holds a live Session JWT at rest (M-3). | [#184](https://github.com/Wide-Moat/open-computer-use/issues/184) |
| P5-I3 | Default egress content-blind; agentic exfil undetectable in transparent mode. | [#182](https://github.com/Wide-Moat/open-computer-use/issues/182) |
| P5-R1 | Non-repudiation of in-sandbox actions depends on guest-authored evidence (M-4) and a trusted clock. | [#181](https://github.com/Wide-Moat/open-computer-use/issues/181), [#185](https://github.com/Wide-Moat/open-computer-use/issues/185) |
| P6-I1 | Default egress content-blind; allow-listed endpoint is a viable exfil channel. | [#182](https://github.com/Wide-Moat/open-computer-use/issues/182) |
| P6-D1 | No per-sandbox egress connection-rate / FD ceiling against the shared edge. | [#188](https://github.com/Wide-Moat/open-computer-use/issues/188) |
| P6-D3 | TTL-only lease expiry and denylist timing clock-dependent. | [#185](https://github.com/Wide-Moat/open-computer-use/issues/185) |
| P6-E3 | Upstreams requiring client-mTLS / cert-pinning / DPoP have no defined edge-injection handling. | [#176](https://github.com/Wide-Moat/open-computer-use/issues/176) |
| P7-S1 | OCSF source field not bound to a host-attested emitter at ingestion (M-4 seam). | [#181](https://github.com/Wide-Moat/open-computer-use/issues/181) |
| P7-R2 | Guest-as-its-own-audit-source needs a mandated out-of-band evidence set (M-4). | [#181](https://github.com/Wide-Moat/open-computer-use/issues/181) |
| P7-T2 | Hash-chain / Merkle cadence and retention floors assume a trustworthy clock. | [#185](https://github.com/Wide-Moat/open-computer-use/issues/185) |
| P7-R3 | Privileged operator actions beyond tier-downgrade not mandatorily audited (M-2). | [#186](https://github.com/Wide-Moat/open-computer-use/issues/186) |
| P7-T3 | Snapshot/hibernation image holds a live session token at rest (M-3). | [#184](https://github.com/Wide-Moat/open-computer-use/issues/184) |

## 5. Machine-checkable layer (tbd)

Threagile threat-model-as-code (DFD-as-YAML, automated STRIDE re-run on DFD-bearing PRs) is deferred until its CI gate lands — [#194](https://github.com/Wide-Moat/open-computer-use/issues/194).

## 6. Open questions

1. Per-tool / per-action authorization and minimum credential-lease scope (G1/G8) — [#187](https://github.com/Wide-Moat/open-computer-use/issues/187).
2. Cross-tenant hardware side-channel: needs a measurable "tenant A cannot observe tenant B" target — [#183](https://github.com/Wide-Moat/open-computer-use/issues/183), [#148](https://github.com/Wide-Moat/open-computer-use/issues/148).
3. Trusted-time / clock-rollback control for TTLs, denylist timing, and the hash-chain / Merkle cadence — [#185](https://github.com/Wide-Moat/open-computer-use/issues/185).
4. Out-of-band evidence for in-sandbox actions and host-attested binding of the OCSF source field at ingestion (M-4) — [#181](https://github.com/Wide-Moat/open-computer-use/issues/181).
5. LINDDUN privacy pass over the same DFD deferred to a follow-on model — [#195](https://github.com/Wide-Moat/open-computer-use/issues/195).
